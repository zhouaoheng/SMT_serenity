<Window x:Class="SMT.NewVersionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SMT"
        mc:Ignorable="d"
        Title="可用更新" Height="300" Width="400" ResizeMode="NoResize" Loaded="Window_Loaded" WindowStartupLocation="CenterOwner">
    <Grid>
        <ScrollViewer>
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="新版本: " FontWeight="Bold" />
                    <Label Content="{Binding NewVersion}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Label Content="当前版本: " FontWeight="Bold" />
                    <Label Content="{Binding CurrentVersion}" />
                </StackPanel>
                <Separator />
                <Label Content="发布说明: " FontWeight="Bold" />
                <Label Content="{Binding ReleaseInfo}" />
                <Separator />
                <Label>
                    <Hyperlink NavigateUri="{Binding ReleaseURL}" RequestNavigate="Hyperlink_RequestNavigate" >下载</Hyperlink>
                </Label>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>